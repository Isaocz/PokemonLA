using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SirfetchdShield : SurroundBaby
{
    // Start is called before the first frame update
    void Start()
    {
        SurroundBabyStart();
    }

    // Update is called once per frame
    void Update()
    {
        SurroundBabyUpdate();
    }
}
